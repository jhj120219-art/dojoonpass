// ================================================================
// 공통 Layout 상수
// docs/FRONTEND_MASTER_SPEC.md §5.2 (중앙 컨테이너 / 최대 폭)
// ================================================================

// 모든 주요 화면(Header 포함)이 공유하는 중앙 컨테이너.
// 1320px는 기존 /search 화면이 이미 쓰고 있던 값을 그대로 표준으로 승격한 것이다 —
// Master Spec §5.2의 "새로운 max-width 값을 임의로 만들지 않는다" 규칙에 따라
// 여기서만 정의하고 각 화면은 이 상수를 가져다 쓴다(화면별 하드코딩 금지).
export const CONTAINER = 'max-w-[1320px] mx-auto px-4 md:px-8'
